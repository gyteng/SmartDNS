var fs = require('fs');

var Packet = require('./packet');

var p = Packet.parse(fs.readFileSync('txt.raw'));

console.log(p.answer)
var p = Packet.parse(fs.readFileSync('long-txt2.raw'));

console.log(p.answer)
